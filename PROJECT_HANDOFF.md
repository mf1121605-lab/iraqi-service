# ملف تسليم السياق — للقراءة من أي أداة ذكاء اصطناعي تفتح هذا المشروع

هذا الملف يوثّق قرارات وسياق مهم **غير موجود بالكود نفسه** — مناقشات ومخططات
اتفق عليها المؤسس مع Claude بجلسة محادثة سابقة، بعضها لسا ما انبنى بالكود.
اقرأه بالكامل قبل أي تعديل، بالإضافة لـ `CLAUDE.md` (بروتوكول العمل الرسمي).

---

## ١. حالة المشروع الحالية (باختصار)

- **الويب** (`src/`): Next.js + Supabase، مستضاف على Vercel، يترفع تلقائياً مع push.
- **الموبايل** (`mobile/`): Expo/React Native، يُبنى فقط عبر
  `eas build --profile preview --platform android` ثم تنصيب APK يدوياً —
  **لا بديل مقبول** (dev-client/Expo Go/WebView/Capacitor/Flutter مرفوضة صراحة).
- **الفرع الوحيد المعتمد:** `claude/direct-edit-github-sync-vdx8mm`.
- **الباك-إند:** Supabase حصراً، لا يوجد Firebase بالمشروع نهائياً.

---

## ٢. إصلاحات حرجة حديثة (لا تعيد تشخيصها من الصفر)

- **كراش قاتل عند فتح التطبيق بعد التسجيل:** كان بسبب
  `mobile/components/ui/AnimatedTopBorderLine.tsx` يستخدم قيمة نسبة مئوية
  (`"25%"`) بخاصية `translateX` — React Native الأصلي يقبل فقط أرقام بكسل
  حقيقية لهذي الخاصية، فكان يرمي استثناء فوري وغير مرئي لأي أداة تشخيص JS.
  تم القياس عبر `onLayout` بدل النسبة المئوية. **مُصلح.**
- **رفع الصور/الفيديو/البصمة الصوتية كان فاشلاً بكل مكان بالتطبيق:** كل
  دوال الرفع كانت تستخدم `response.blob()` — طريقة غير موثوقة على React
  Native (ترفع أحياناً ملف فاضي بدون خطأ ظاهر). تم التحويل لـ
  `response.arrayBuffer()` (الطريقة الموصى بها من Supabase نفسها لـ RN)
  بكل الأماكن العشرة اللي فيها رفع ملفات. **مُصلح.**
- **أيقونة التطبيق كانت تنقص (تُقصّ) رغم إصلاح سابق:** الإصلاح الأول قاس
  فقط عرض/ارتفاع المحتوى، مو المسافة القطرية من المركز — أطراف جناحي
  النسر كانت توصل لـ١٢٠٪ من نصف قطر المنطقة الآمنة. تم إعادة الحساب
  والتصغير لـ٥٨٪ (الآمن ≤٦١٪)، وتحقق بصري بمحاكاة قناع دائري حقيقي.
  **مُصلح.**
- **شاشة تسجيل الدخول كان النموذج فيها يختفي كلياً:** كان مغلّف بـ
  `Animated.View` معتمد على تأخير ٦٨٠ مللي ثانية قبل يوصل لـ opacity=1 —
  لو تأخر أو فشل، يبقى النموذج شفاف بالكامل لكن ياخذ مساحته. تم إزالة هذا
  التأثير كلياً، النموذج الآن يظهر فوراً وبثبات. **مُصلح ومتحقق منه بقراءة
  الكود مرتين.**

---

## ٣. خطة "الأستاذ رشدي" (المدرس الذكي) — **مخطط بالكامل، ما انبنى بالكود بعد**

هذي أهم قسم بالملف — نقاش طويل صار بمحادثة سابقة وصل لتصميم كامل، بس
**لا يوجد أي سطر كود منه بالمستودع حالياً**. الميزة الحالية الوحيدة الموجودة
فعلياً هي `src/pages/api/tutor/send-message.js` (تستخدم Anthropic API،
`claude-haiku-4-5-20251001`) — هذي **نسخة أولية قديمة**، القرار الأخير كان
التحويل لبنية جديدة كلياً:

### القرارات المعمارية النهائية المتفق عليها
1. **نموذج اللغة:** التحويل من Anthropic إلى **Groq API**
   (`openai/gpt-oss-120b` — نفس النموذج المستخدم فعلاً بميزة تحليل الأخبار
   `src/pages/api/hq/parse-announcement.js`، فمفتاح API موجود أصلاً).
   السبب: أرخص بكثير (٠.١٥$/٠.٦$ لكل مليون توكن مقابل ١$/٥$ لـ Anthropic
   Haiku)، وسريع جداً (عتاد LPU)، ونفس نمط الربط مثبت أصلاً بالمشروع.
2. **دقة علمية عبر RAG:** محتوى المنهج الدراسي يُخزّن بجدول Supabase خاص
   بنا (مو يُرفع لـ Groq — Groq لا تملك أي ميزة تخزين/رفع ملفات، هي فقط
   محرك استدلال). عند كل سؤال، نبحث بمحتوى المنهج المخزّن عندنا، ونرسل
   الجزء المرتبط فقط مع سؤال الطالب لـ Groq.
3. **اللغة:** الشرح بالفصحى بشكل أساسي، مع لهجة عراقية فقط بلحظات محددة
   (تشجيع/تفاعل) — المؤسس يزوّدنا بقائمة عبارات عراقية (مثل "عاشت ايدك"،
   "صدك تحجي؟") ومتى تُستخدم كل وحدة.
4. **الصوت:**
   - **الصوت العراقي الأصيل:** Azure Speech — يوجد صوتان عراقيان حقيقيان
     بلهجة "كِلِت" العراقية موثقان رسمياً: `ar-IQ-BasselNeural` (رجالي)
     و`ar-IQ-RanaNeural` (نسائي). السعر: ١٦$ لكل مليون حرف، ٥٠٠ ألف حرف
     مجانية شهرياً.
   - **قرار مهم لتخفيض التكلفة:** لا نُسرد كل رد بالصوت تلقائياً (هذا كان
     سيكلف ~٦٠٠-٧٠٠$/شهر لـ٢٠٠ طالب). بدلاً من هذا: العبارات العراقية
     القصيرة الثابتة (نفس القائمة من نقطة ٣) تُولّد **مرة واحدة فقط** عبر
     **ElevenLabs** (يُفضّل استخدام Instant Voice Cloning بتسجيل صوت
     عراقي حقيقي دقيقة واحدة لضمان لهجة أصيلة، لأن ElevenLabs ما عنده
     صوت عراقي رسمي معتمد بعكس Azure)، تُحفظ كملفات صوتية جاهزة بالتطبيق،
     وتُشغّل من الذاكرة بدون أي استدعاء API متكرر. الشرح الفعلي (الفصحى)
     يبقى **نص فقط بدون صوت**.
5. **الفيديو:** المؤسس عنده فيديوهات جاهزة صمّمها للشخصية (كل فيديو
   يمثّل حالة/تعبير معين). لا نحتاج أي خدمة AI-avatar مكلفة (D-ID/HeyGen) —
   فقط تزامن بسيط بالكود: فيديو "يتكلم" يشتغل تلقائياً أثناء تشغيل الصوت،
   يرجع لفيديو "ساكت" لما يخلص. **المؤسس لسا ما أرسل الفيديوهات ولا قائمة
   العبارات وأسماء الحالات** — مطلوبة منه قبل البدء بهذا الجزء.
6. **نظام الاختبارات (Quizzes) — مخطط، أوسع من كل ما سبق:**
   - متابعة مستوى كل طالب بكل مادة على مدار السنة الدراسية (جدول جديد).
   - دورة: شرح → تعليم → اختبار، مبني فعلياً على محتوى المنهج (RAG).
   - واجهة اختبار احترافية: صندوق أسئلة اختيار من متعدد، **١٠ أسئلة لكل
     اختبار، كل سؤال بعدد اختيارات اعتيادي (مثلاً ٤)** — تم توضيح هذا
     صراحة من المؤسس، لا يقصد ١٠ اختيارات بالسؤال الواحد.
   - الشخصية (الأستاذ رشدي) تظهر خلف صندوق الاختبار وتتفاعل: تعبير فرح
     عند الإجابة الصحيحة، تعبير آخر عند الخطأ (يربط بنظام الفيديو أعلاه).
   - نتيجة نهائية شاملة بعد آخر سؤال.
   - **اتفقنا نبني الأساس (الشرح + الصوت) أولاً بمادة وحدة كتجربة، ثم
     نضيف نظام الاختبارات والمتابعة كدفعة منفصلة لاحقاً — لا تبني كل شي
     دفعة وحدة.**

### الخطوات التالية المطلوبة فعلياً (بالترتيب)
1. المؤسس يرسل محتوى منهج مادة وحدة (تجربة أولية) — لم يُرسل بعد.
2. بناء جدول المنهج + آلية البحث/الاسترجاع (RAG) بـ Supabase.
3. تحويل `send-message.js` من Anthropic لـ Groq + دمج RAG.
4. تجربة جودة الشرح بالعربي/الفصحى قبل التوسع لبقية المواد.
5. لاحقاً: الصوت (Azure + ElevenLabs)، الفيديو، نظام الاختبارات.

---

## ٤. نظام تخصيص التصميم لكل عنصر (Design Mode) — **دفعة أولى منجزة فقط**

المؤسس طلب: يضغط على **أي زر/نص/صندوق بالتطبيق كل واحد لحاله** ويغيّر لونه/
حجمه/نوع خطه (أو لون خلفية الصناديق)، بدون حاجة لطلب تعديل مني كل مرة.
وافق صراحة إنه عمل كبير، وطلب أخذ الوقت الكافي وتجنب الأخطاء.

**المُنجز (Batch 1):**
- جدول `ui_style_overrides` بـ Supabase (قراءة عامة، كتابة للمؤسس فقط).
- ٥ خطوط عربية مُدمجة بالتطبيق يختار المؤسس بينها: Cairo, Tajawal,
  Almarai, IBM Plex Sans Arabic, Noto Kufi Arabic.
- زر عائم 🎨 يظهر للمؤسس فقط، يفعّل "وضع التصميم".
- مكوّنات `ThemedText`/`ThemedBox` (بديل لـ `Text`/`View` العادية) —
  تقرأ التخصيص المحفوظ وتفتح محرر عند الضغط بوضع التصميم.
- محرر تخصيص (`ElementEditorSheet`): ألوان جاهزة + حقل hex مخصص + حجم
  خط بأزرار +/- + اختيار نوع خط. **بدون مكتبات slider/color-picker
  خارجية عمداً** (تفادي مخاطر native dependencies جديدة).
- **مُطبّق تجريبياً على شاشة تسجيل الدخول فقط** (العنوان، رسائل الخطأ،
  الروابط، زر غوغل، أزرار من نحن/الخصوصية).

**غير منجز بعد (دفعات قادمة):**
- تعميم `ThemedText`/`ThemedBox` على باقي شاشات التطبيق (عشرات الملفات —
  لوحة المؤسس، لوحة الموظف، الدردشة، الأخبار، البروفايل...).
- بعض الصناديق المركّبة (مثل `GoldCard`) تحتاج تعديل داخلي أول قبل ما
  تقبل تخصيص لون الخلفية عبر النظام — لأنها ترسم تدرّج لوني فوق أي لون
  خلفية تلقائياً حالياً.

---

## ٥. تنسيق بين أدوات ذكاء اصطناعي متعددة — **مهم جداً**

المؤسس يستخدم بالتوازي: هذي الجلسة، Claude داخل VS Code، **وأداة اسمها
Codex اكتُشفت حديثاً** (موجودة كـ git worktree على جهازه بمسار
`.codex\worktrees\825b\iraqi-service`). حصلت مشكلة فعلية سابقاً: أداة VS
Code شغّلت `eas init` بدون علم أحد وأنشأت مشروع Expo منفصل ونشرت تحديثات
لقنوات خاطئة — تم اكتشافها وإصلاحها.

**قواعد صارمة لأي أداة تعمل على هذا المشروع:**
- `git pull` من `claude/direct-edit-github-sync-vdx8mm` قبل أي تعديل، دائماً.
- ممنوع: `eas init`، `eas update:configure`، إنشاء remote جديد، إنشاء
  worktree جديد، تعديل `app.json`/`eas.json` (خصوصاً `projectId`/`owner`/
  `updates.url`) بدون سؤال المؤسس أولاً.
- ممنوع تشغيل `eas build`/`eas update` من داخل الأداة نفسها — البناء
  يسويه المؤسس يدوياً من PowerShell على جهازه فقط.
- إذا لاحظت remote أو worktree أو فرع غريب لم تُنشئه أنت — **لا تحذفه ولا
  تتجاهله، أخبر المؤسس فوراً واسأل قبل أي إجراء.**
