# بروتوكول العمل: "الخريطة السحرية" (Magic Roadmap Protocol)

هذا الملف يوثّق طريقة العمل المتفق عليها بين المؤسس (صاحب المشروع) و Claude، بحيث تبقى محفوظة حتى لو بدأت جلسة عمل جديدة لاحقاً.

## آلية العمل

1. **التخطيط:** المؤسس يرسل مجموعة تحديثات/إصلاحات/إضافات مطلوبة. Claude يحلّلها بدقة ويبني **خريطة عمل (Roadmap)** مقسّمة إلى **دفعات (Batches)** متسلسلة ومنطقية.
2. **التنفيذ التكراري:** التنفيذ يصير دفعة بعد دفعة، بأقصى دقة ممكنة.
   - **شرط الصراحة:** أي جزئية ما تم تنفيذها بدقة 100% لأي سبب، يجب إخبار المؤسس بها صراحةً مع تحديد النسبة المنجزة والسبب — بدون تجميل أو إخفاء.
3. **الإنهاء والتسليم:** عندما يرسل المؤسس العبارة الحرفية:
   > "قم بأنهاء عملية الخريطة السحرية وارسل الصناديق والتحديثات"

   يجب تنفيذ الآتي فوراً:
   - **رفع الكود:** التأكد من رفع كل التعديلات لفرع GitHub الحالي (`claude/direct-edit-github-sync-vdx8mm`).
   - **صندوق SQL واحد:** يجمع كل استعلامات/تحديثات SQL من كل الدفعات المنفذة بالجلسة، جاهزة للنسخ دفعة واحدة وتشغيلها بـ Supabase SQL Editor.
   - **صندوق PowerShell واحد:** سكربت جاهز للصق يسوي `git pull` + `eas build --profile preview --platform android`.

## حدود قدرات Claude في هذا البروتوكول (متفق عليها مع المؤسس)

- **لا يوجد Firebase بهذا المشروع** — الباك-إند هو Supabase، والموقع مستضاف على Vercel (يترفع تلقائياً مع كل push لو مربوط)، وتطبيق الموبايل يُبنى عبر EAS. لا خدمة ثالثة تحتاج رفع يدوي.
- **Claude لا يستطيع تشغيل `eas build` من عنده ولا الوصول لحساب Expo الخاص بالمؤسس** — سكربت PowerShell يُشغَّل من جهاز المؤسس فقط، ورابط APK الناتج يظهر مباشرة بنافذة PowerShell عنده هو، وليس شيئاً يقدر Claude يجيبه أو يرسله تلقائياً.

## قيود عمل ثابتة إضافية (من محادثات سابقة، لا تتغير)

- **طريقة البناء الوحيدة المقبولة:** `eas build --profile preview --platform android` ثم تنصيب APK يدوياً. **ممنوع اقتراح** dev-client أو Expo Go أو أي تطبيق WebView/Capacitor أو Flutter كبديل — تم تجربتها ورفضها صراحة من المؤسس.
- **إرسال ملفات SQL:** ترسل كنص/كود داخل المحادثة مباشرة (ليس كملف مرفق فقط) عند الطلب — تفضيل المؤسس بسبب صعوبة فتح المرفقات على الموبايل.
- **حساسية لصق SQL بالموبايل:** أوامر الـ SQL المرسلة للمؤسس يُفضّل أن تكون **سطر واحد** حيث ممكن، لتفادي أخطاء نسخ/لصق شائعة (فقدان علامات اقتباس، مسافات بادئة تراكمية) عند اللصق داخل محرر Supabase على الموبايل.
- **تعديلات الصلاحيات (GRANT/REVOKE) على Supabase:** أحياناً لا تنعكس فوراً على طبقة PostgREST (الـ API) حتى لو نجحت بقاعدة البيانات مباشرة. الحل: تشغيل `NOTIFY pgrst, 'reload schema';` مرة واحدة بعد أي تعديل GRANT/REVOKE لإجبار الطبقة الوسيطة على التحديث الفوري والدائم.
