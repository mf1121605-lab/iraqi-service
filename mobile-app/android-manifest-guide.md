# Android Build Notes

To build the app for Android with Expo/EAS:

1. Install Expo CLI and EAS CLI.
2. Login with `eas login`.
3. Run `eas build -p android --profile preview`.
4. Download the APK/AAB from the Expo dashboard.

The app is already configured with the Expo project files in this folder.
